import { Module } from '@nestjs/common';

/**
 * Notification Module Skeleton
 * Scope: Multi-channel messaging orchestration (SMS, IVR, USSD) leveraging NotificationProvider.
 * Business logic will be implemented in subsequent phases.
 */
@Module({})
export class NotificationModule {}
